Calculator:
Program which solves the arithmetic operation with following operators;
+, -, /, %, ^, * and ()

AllSubsets:
Program to print all subsets upto a given number(taken as input from command line argument).
Ex:3
output: Q(empty), (1), (2), (3), (1, 2), (2, 3), (1, 3) and (1 ,2, 3)

Factorization:
A number is taken as a input from command line argument and all prime factors of that number are printed as output
